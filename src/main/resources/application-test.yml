# 사용자 정의 속성
profile:
  value: 'test'

spring:
  datasource:
#    url: ${TEST_DB_URL}
#    username: ${TEST_DB_USERNAME}
#    password: ${TEST_DB_PASSWORD}
    url: jdbc:mysql://localhost:3306/pettopiadb
    username: sh
    password: sh
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    generate-ddl: false
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        format_sql: true